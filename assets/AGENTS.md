# assets/ — 规则层

继承根规则，见 [../AGENTS.md](../AGENTS.md)。

只讲**在这里要怎么干**；目录里有什么、被谁引用、图长什么样 → [README.md](README.md)。

## 什么时候必须重截

判据是「卡片渲染出来的东西有没有变」，不是「改了哪个文件」。
与 [release-guard.mjs](../scripts/release-guard.mjs) 同一种分工：它只判路径，行为等价的改动交给人回答。

必须重截：

- `src/client/locales.ts` 两份字典的**文案值**（中英任一）
- `src/client/index.tsx` 的 **JSX 结构**：字段增删、顺序、按钮、状态行
- `src/client/index.tsx` 的**样式对象**：字号、行高、间距、令牌、布局
- 卡片**可见状态**的语义：未保存标记、已配置 / 未配置徽标、已覆盖 + 重置、只读遮蔽行、失败诊断

不必重截：

- 类型与注释（只活在编译期）
- 行为等价的内部重构：抽常量、改名、等价重写
- `README` / `AGENTS` / 测试 / 构建配置
- `inject`、注册 key、`dsh.client.*`：它们坏了是「卡片装不上」，不是「图不准」

拿不准时按「必须重截」办：图的成本低于一张与界面不符的图。

## 完成定义

- 改渲染面的改动，**要么同批带重截图，要么在 commit body 里写明为什么不重截**。
- 图与文案分成两个提交，会留下「文案已改、README 里的图还是旧的」的窗口。

## 怎么截

截图会改**全局设置**（语言等），所以第 0 步是问，不是动手。

0. **先问再动**，问的时候说清三件事，再等维护者确认：
   - 要占用浏览器几分钟；
   - 会临时切界面语言，结束切回；
   - **截图期间请别改语言 / 主题 / 模型等全局设置** —— 它们直接进画面。
1. **用自己的标签页，不动维护者的标签页**：agent 端点新开一个页面打开 GUI 即可（实测与原标签页并存、登录态照常），结束时关掉它。
2. **开一个新会话**，再按 [README.md](README.md) 的「风格基准」把画面摆到位；摆好后**不再动任何别的东西**。
3. 进侧边栏 **Plugins → Installed → `zhihu-search`**（详情页里的短名是 `zhihu-search`，不是全包名），
   **再点该插件那一行的 Configure** —— 接缝换代后卡片住在**行配置页**上（`plugins.row.config`），
   不在插件详情页里；面包屑会是「插件名 → 行名」，这正是旧图（详情页里的配置区）与真实入口不符的地方。
   进了这个页面后滚到配置区完整可见（底部**唯一的保存按钮**必须在图内）。
   原生配置表单**不可折叠**：没有卡片折叠头可展开，字段直接铺在行配置页的 `[data-plugin-config]` 区里；「放弃」按钮也不存在，草稿随离开页面丢弃。
4. **按快门前收起左侧栏**（隐私硬规则，独立一步）：展开的左侧栏列的是维护者真实的**会话标题与工作区名** —— 那是别人的工作内容，不是装饰，门面图不能带出去。
   收起**是带动画的**，等固定时长不算保证（本次重拍踩过：`settings-card_en.png` 一度就是收到一半的中间帧）。
   点收起后**轮询到几何稳定再按快门**：侧栏容器的 `getBoundingClientRect().width` **连续两次采样相同**（间隔约 100ms）、已出现展开按钮（`Open sidebar` / 中文等价）、且宽度落在收起态区间；
   轮询给上限（约 5 秒），**超时按报错处理、重来一次，不许拿中间帧凑数**。
   同类动作一律照此办理：**切语言、切视图、展开或收起折叠区**之后也要轮询到稳定，别用固定等待当保证。
   收起状态下点不到 `Plugins`，所以顺序是死的：**展开导航 → 走到目标画面 → 收起侧栏 → 按快门**，不能图省事一开始就收。
5. **开关状态照现状拍**：拨它会带出「未保存」标记，真保存又改了维护者的真实配置。
6. 界面语言**当前是 English**：先拍英文版，再临时切成中文拍中文版，最后**把语言切回 English**。切语言会让页面重载，切完重新走一遍第 3、4 步。
7. **交还**：关掉自己那个标签页 → 把当前会话切回维护者原来那个（会话选择会被记住，下次开页面落在它上面）→ 告诉他可以用了。

## 已知约束（重拍实测）

- **整屏图不要设置 viewport**：用**自然视口**拍，产出的物理尺寸自然就是 **2449x1223**（CSS 视口 1484x741 × devicePixelRatio 1.65）。
  **不要 `setViewportSize(2449, 1223)`** —— 它会把 dpr 压成 1，界面只剩原生约 60%，正是维护者当场指出的「界面缩小了」。2449x1223 是**物理像素结果**，不是要设的 CSS 视口。
- **devicePixelRatio 本来就是 1.65**：不要去动它，也不要把它压成 1。靠 CDP 改密度那条路被 TabbitDance 拒（`Command rejected`），所以 1.65 是唯一正常值，2x 不可得。
- **只有拍长元素时才临时调 viewport，且只调高度**（宽度不要动），拍完立刻还原。
- **长元素要两步拍**：先解除祖先裁剪（从锚点往上把 `overflow` 清成 `visible`、`max-height` 清成 `none`，直到 `body`），再对**最小祖先**做元素级截图。
  `locator.screenshot({ path: '<绝对路径>.png' })` 是原生 Playwright，**会照写你给的路径**；`page.screenshot()` 会被浏览器运行时接管、落进它的 artifacts 目录，真实路径只能从返回值里取。
- **落盘一律绝对路径**：tabbit 的 nodejs 运行器里 `process.env.TEMP` 是 `undefined`，相对路径会写进浏览器安装目录。

## 验收

- 配置区完整：**插件标题与描述**（由插件页自己画）、三个字段行、状态徽标（已配置 / 未配置）、说明行、**唯一的保存按钮** —— 一样不缺
- 出现折叠头、「放弃」按钮或卡片外框，说明拍的是旧界面
- **图里左侧栏是收起态**：看不到任何会话标题、工作区名或对话内容（本仓没有例外）
- **侧栏是动画中间帧 / 宽度不固定 → 判废重拍**（稳定性判据见第 4 步的轮询）
- 说明文案与当前 `locales.ts` **逐字一致**
- 尺寸与风格符合 [README.md](README.md) 的「风格基准」
