# utils/ — 规则层

继承根规则，见 [../../AGENTS.md](../../AGENTS.md)。

本目录特有约束：

- 全部导出必须是**纯函数**：不得读时钟、环境变量、缓存或网络。可测性依赖这一点。
- 禁止运行时 import `@deepseek-ai/*`，类型一律 `import type`。
- 日期必须先归一化到 UTC 再转时间戳，否则同一输入在不同机器产生不同缓存键。
- 编译失败一律抛 `CompileError` 并带 `hint`，不要静默产出坏字符串。
